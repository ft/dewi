set_opt('debug', 'yes');
register({ glob        => [ qw{ alice bar barney baz bob foo fred } ],
           method      => 'filtered',
           concatenate => 'yes_please',
           destination => '~/.concatenate/config' });
end();

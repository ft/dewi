set_opt('debug', 'yes');
register({ glob        => [ qw{ alice bar barney baz bob foo fred } ],
           method      => 'filtered',
           concatenate => 'config',
           destination => '~/.concatenate' });
end();
